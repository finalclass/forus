<%= form_for @conn, accounts_session_path(@conn, :create), [method: :post], fn f ->  %>
<%= forus_text_field(f, :email) %>
<%= forus_password_field(f, :password) %>

<div class="field">
  <div class="control">
    <%= submit "Login", class: "button is-primary" %>
  </div>
</div>
<% end %>
